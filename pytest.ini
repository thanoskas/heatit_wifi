[pytest]
asyncio_mode = auto
testpaths = tests
# test_integration.py drives a live Home Assistant container (see the
# GitHub workflow); the unit tests run against a mocked device instead.
addopts = --ignore=tests/test_integration.py
